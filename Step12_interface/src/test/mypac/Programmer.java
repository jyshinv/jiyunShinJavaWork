package test.mypac;

public interface Programmer {
	//자동차를 만든다고 하자.
	//아래처럼 표준을 정해주면 이 안에서는 마음대로 정의해서 사용할 수 있다.
	//인터페이스는 이런식으로 표준을 정해준다. 
	public void 헤드라이터();
	public void 바퀴();
	public void 본체();
}
